# Dev routes — host processes (Vite + uvicorn) and TLS for *.localhost.

http:
  routers:
    fast-next-backend-host:
      rule: Host(`api.localhost`)
      entryPoints:
        - http
        - https
      service: fast-next-backend-host
    fast-next-frontend-host:
      rule: Host(`dashboard.localhost`)
      entryPoints:
        - http
        - https
      service: fast-next-frontend-host

  services:
    fast-next-backend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:8000
    fast-next-frontend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:3100

tls:
  stores:
    default:
      defaultGeneratedCert:
        resolver: default
        domain:
          main: localhost
          sans:
            - "*.localhost"
